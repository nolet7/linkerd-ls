apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linkerd-viz
  namespace: argocd
spec:
  project: platform-tools
  destination:
    server: https://kubernetes.default.svc
    namespace: linkerd-viz
  sources:
    - repoURL: oci://ghcr.io/linkerd/charts
      chart: linkerd-viz
      targetRevision: "2.14.0"
      helm:
        valueFiles:
          - $values/values/values-dev.yaml
    - repoURL: https://github.com/nolet7/helm
      targetRevision: HEAD
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

